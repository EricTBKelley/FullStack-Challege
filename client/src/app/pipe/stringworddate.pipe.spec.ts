import { StringworddatePipe } from './stringworddate.pipe';

describe('StringworddatePipe', () => {
  it('create an instance', () => {
    const pipe = new StringworddatePipe();
    expect(pipe).toBeTruthy();
  });
});
